import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";const read=p=>readFile(new URL(p,import.meta.url),"utf8");test("admin read contracts include accessible client filtering",async()=>{const pages=await Promise.all(["../src/pages/DashboardPage.jsx","../src/pages/UsersPage.jsx","../src/pages/DriversPage.jsx","../src/pages/RidesPage.jsx","../src/pages/LiveMapPage.jsx"].map(read));const s=pages.join("\n").toLowerCase(),users=pages[1];for(const token of["totalrides","bystatus","/drivers/available",".drivers","aggregate-only","driver.location.updated","new map","latitude","longitude"])assert.ok(s.includes(token),token);assert.match(users,/<input[^>]+value=/);assert.match(users,/onChange=/);assert.match(users,/rows\.filter/);assert.ok(!s.includes("client.get(\"/rides\")"));});