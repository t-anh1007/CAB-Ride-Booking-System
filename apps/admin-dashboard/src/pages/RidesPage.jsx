import{useEffect,useState}from"react";import{useAuth}from"../providers/AuthProvider.jsx";import DataState from"../components/DataState.jsx";export default function RidesPage(){const{client}=useAuth();const[stats,setStats]=useState(null),[error,setError]=useState("");useEffect(()=>{let active=true;client.get("/rides/stats").then(result=>active&&setStats(result.data||result)).catch(e=>active&&setError(e.message));return()=>{active=false}},[client]);return <section><h1>Chuyến đi</h1>{error&&<DataState error={error}/>} {!error&&!stats&&<p className="muted">Đang tải…</p>} {stats&&<div className="card"><p>Tổng quan aggregate-only, read-only từ endpoint thống kê hiện có.</p><strong>{stats.totalRides??0} chuyến</strong><p className="muted">Danh sách từng chuyến chưa có endpoint GET /rides công khai, nên CAB không hiển thị dữ liệu giả.</p></div>}</section>}