import{useEffect,useMemo,useState}from"react";import{useAuth}from"../providers/AuthProvider.jsx";import DataState from"../components/DataState.jsx";export default function UsersPage(){const{client}=useAuth();const[rows,setRows]=useState(null),[query,setQuery]=useState(""),[error,setError]=useState("");useEffect(()=>{let active=true;client.get("/users").then(x=>active&&setRows(x.data||x.items||x)).catch(e=>active&&setError(e.message));return()=>{active=false}},[client]);const filtered=useMemo(()=>Array.isArray(rows)?rows.filter(row=>JSON.stringify(row).toLowerCase().includes(query.toLowerCase())):[],[rows,query]);return <section><h1>Người dùng</h1><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Tìm người dùng" aria-label="Tìm người dùng"/>{error&&<DataState error={error}/>} {!error&&!rows&&<p className="muted">Đang tải…</p>} {Array.isArray(rows)&&<table className="table"><thead><tr><th>Id</th><th>Name</th><th>Phone</th></tr></thead><tbody>{filtered.map((row,i)=><tr key={row.id||i}><td>{row.id??"—"}</td><td>{row.name??"—"}</td><td>{row.phone??"—"}</td></tr>)}</tbody></table>}</section>}