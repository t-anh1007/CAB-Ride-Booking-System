import{useEffect,useState}from"react";import{useAuth}from"../providers/AuthProvider.jsx";import DataState from"../components/DataState.jsx";export default function DriversPage(){const{client}=useAuth();const[rows,setRows]=useState(null),[error,setError]=useState("");useEffect(()=>{let active=true;client.get("/drivers/available").then(result=>{const payload=result.data||result;if(active)setRows(payload.drivers||payload.items||payload)}).catch(e=>active&&setError(e.message));return()=>{active=false}},[client]);return <section><h1>Tài xế đang khả dụng</h1>{error&&<DataState error={error}/>} {!error&&!rows&&<p className="muted">Đang tải…</p>} {Array.isArray(rows)&&<table className="table"><thead><tr><th>Id</th><th>Name</th><th>Status</th></tr></thead><tbody>{rows.map((row,i)=><tr key={row.driverId||row.id||i}><td>{row.driverId??row.id??"—"}</td><td>{row.name??row.fullName??"—"}</td><td>{row.status??row.availability??"—"}</td></tr>)}</tbody></table>}</section>}
