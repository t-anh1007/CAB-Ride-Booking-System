import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";const read=p=>readFile(new URL(p,import.meta.url),"utf8");test("driver operational contracts include safe availability toggle",async()=>{const pages=await Promise.all(["../src/pages/DriverHomePage.jsx","../src/pages/IncomingRequestPage.jsx","../src/pages/ActiveRidePage.jsx","../src/pages/EarningsPage.jsx","../src/pages/DriverHistoryPage.jsx"].map(read));const s=pages.join("\n").toLowerCase(),home=pages[0];for(const token of["/location","currentlocation","watchposition","5000","pricesnapshot","rideid","realtime","completed","/cancel","go-online","go-offline","pending","error"])assert.ok(s.includes(token),token);assert.match(home,/try\s*\{/);assert.match(home,/catch\s*\(/);assert.match(home,/disabled=\{pending\}/);});