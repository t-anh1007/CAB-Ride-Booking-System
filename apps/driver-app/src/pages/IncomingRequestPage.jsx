import{useEffect,useState}from"react";import{useLocation,useNavigate}from"react-router-dom";import{useAuth}from"../providers/AuthProvider.jsx";export default function IncomingRequestPage(){const{state}=useLocation(),nav=useNavigate(),{client,user}=useAuth();const ride=state?.ride||{};const[remaining,setRemaining]=useState(15),[error,setError]=useState("");useEffect(()=>{const timer=setInterval(()=>setRemaining(x=>Math.max(0,x-1)),1000);return()=>clearInterval(timer)},[]);const accept=async()=>{try{const id=ride.rideId||ride.id;await client.post("/rides/"+id+"/accept",{});nav("/ride/"+id)}catch(e){setError(e.message)}};const reject=async()=>{try{const id=ride.rideId||ride.id;await client.post("/rides/"+id+"/cancel",{driverId:user?.id,reason:"Driver declined request"});nav("/home")}catch(e){setError(e.message)}};return <main className="page"><h1>Yêu cầu mới</h1><div className="card"><p>Hết hạn sau {remaining}s</p><p>{ride.pickup?.label||"Điểm đón mới"}</p><button disabled={!ride.rideId&&!ride.id} onClick={accept}>Nhận chuyến</button><button disabled={!ride.rideId&&!ride.id} className="danger" onClick={reject}>Từ chối</button>{error&&<p className="state">{error}</p>}</div></main>}
