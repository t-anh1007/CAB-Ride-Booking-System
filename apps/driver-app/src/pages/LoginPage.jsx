import{useState}from"react";import{useNavigate}from"react-router-dom";import{useAuth}from"../providers/AuthProvider.jsx";export default function LoginPage(){const[phone,setPhone]=useState(""),[otp,setOtp]=useState(""),[sent,setSent]=useState(false),[error,setError]=useState("");const{requestOtp,verifyOtp}=useAuth(),nav=useNavigate();const submit=async()=>{try{if(!sent){await requestOtp(phone);setSent(true)}else{await verifyOtp(phone,otp);nav("/home")}}catch(e){setError(e.message)}};return <main className="page"><h1>Đăng nhập tài xế</h1><div className="card"><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Số điện thoại"/>{sent&&<input value={otp} onChange={e=>setOtp(e.target.value)} placeholder="Mã OTP"/>}<button onClick={submit}>{sent?"Xác minh":"Gửi OTP"}</button>{error&&<p className="state">{error}</p>}</div></main>}
