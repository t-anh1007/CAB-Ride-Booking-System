import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import{reverseGeocode}from"../src/geo/nominatim.js";test("reverse geocode has a safe fallback and shared barrel declares its export",async()=>{assert.equal(await reverseGeocode(10,106,{fetchImpl:async()=>{throw Error("offline")}}),null);const barrel=await readFile(new URL("../src/index.js",import.meta.url),"utf8");assert.ok(barrel.includes("reverseGeocode"));assert.ok(barrel.includes("./geo/nominatim.js"));});