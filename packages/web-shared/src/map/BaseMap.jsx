import{MapContainer,TileLayer,useMapEvents}from"react-leaflet";import"leaflet/dist/leaflet.css";function Click({onMapClick}){useMapEvents({click:e=>onMapClick?.([e.latlng.lat,e.latlng.lng])});return null}export function BaseMap({center=[10.7769,106.7009],zoom=14,onMapClick,children,className="map"}){return <MapContainer center={center} zoom={zoom} className={className}><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://tile.openstreetmap.org/{z}/{x}/{y}.png"/><Click onMapClick={onMapClick}/>{children}</MapContainer>}
