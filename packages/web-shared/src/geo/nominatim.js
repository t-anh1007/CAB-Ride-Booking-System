let serial=Promise.resolve(),nextAt=0;export function searchAddress(query,{fetchImpl=fetch,now=()=>Date.now(),sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms))}={}){const task=async()=>{const delay=Math.max(0,nextAt-now());if(delay)await sleep(delay);nextAt=now()+1000;const url="https://nominatim.openstreetmap.org/search?format=json&limit=5&q="+encodeURIComponent(query);const rows=await(await fetchImpl(url,{headers:{"User-Agent":"CAB-Booking-System/1.0"}})).json();return rows.map(row=>({label:row.display_name,lat:Number(row.lat),lng:Number(row.lon)}))};const result=serial.then(task,task);serial=result.catch(()=>{});return result}export async function reverseGeocode(lat,lng,{fetchImpl=fetch}={}){try{const url="https://nominatim.openstreetmap.org/reverse?format=json&lat="+encodeURIComponent(lat)+"&lon="+encodeURIComponent(lng);const response=await fetchImpl(url,{headers:{"User-Agent":"CAB-Booking-System/1.0"}});if(!response.ok)return null;const row=await response.json();return row.display_name||null}catch{return null}}